# Web Proxy Adapter モックサーバー設計

## 1. 目的
外部 API 依存なしでのテスト環境構築。

## 2. 設計
*   **モックモード**: `WebProxyConfig` に `MOCK_SERVER` タイプを追加。
*   **ローカルサーバー**: `httptest.Server` を利用し、設定されたレスポンスを返す。
*   **統合テスト**: 認証ロジックの検証をローカルで完結させる。
