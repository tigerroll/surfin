-- Create batch_metadata schema if it doesn't exist
CREATE SCHEMA IF NOT EXISTS batch_metadata;

-- Set search path to batch_metadata for subsequent operations
SET search_path TO batch_metadata;

-- 0001_initial_schema.up.sql: Initial framework schema for PostgreSQL
